[project]
name = "reinspiring-oracle"
version = "0.1.0"
description = "Reference oracle for ReinspiRING (Algorithm 2) at small parameters. See ../../SPEC.md."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "sympy>=1.13",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.15.12",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["reinspiring_oracle"]

[tool.ruff]
target-version = "py311"
src = ["reinspiring_oracle", "tests"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
